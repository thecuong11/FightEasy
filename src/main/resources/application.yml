spring:
  datasource:
    url: jdbc:postgresql://postgres:5432:5432/fighteasy
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
